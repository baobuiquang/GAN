#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

#define target 1000

int main()
{
	int sum = 0;
	for (int i = 1; i < target; i++)
	{
		if ((i % 3 == 0) || (i % 5 == 0))
		{
			sum += i;
		}
	}
	printf("Sum = %d", sum);
	return 0;
}
